
.loading(ng-show='loading')
  .container
    h1.title Loading

.login(ng-show='login',ng-hide='loading')
  .container
    form(ng-submit='joinRoom()')
        input(type='text',ng-model='username')
        button.send(type='submit') join


.chat-room(ng-hide='login || loading')
  .container

    h1.title Room #<span class='room'>{{roomInfo.room}}</span>

















    //- ul.messages
    //-   li.title-bar
    //-     .user User
    //-     .message Message
    //-     .clearfix

    //-   li.message(ng-repeat="message in messages")
    //-     .user {{message.user}}
    //-     .message {{message.message}}
    //-     .clearfix

    //-   li.new-message

    //-     form(ng-submit="sendMessage()")

    //-       .user
    //-         input(type='text',name='user',ng-model='currentUser')

    //-       .message
    //-         input(type='text',name='message',ng-model='currentMessage')

    //-       button.send(type='submit') send

    //-       .clearfix
